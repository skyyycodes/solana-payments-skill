{
  "name": "subscription-program-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bankrun (clock-warped) integration tests for the reference subscription program. Requires the Anchor + Solana toolchain and `anchor build`.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.98.4",
    "anchor-bankrun": "^0.5.0",
    "solana-bankrun": "^0.4.0",
    "vitest": "^2.1.0"
  }
}
